#!/bin/sh

message="${1:-"<- ran script"}"
script_name="$(basename $0)"
current_date=$(date -u +"%Y-%m-%d %H:%M")

function loggerd() {
	printf "%s-%s-%s\n" "$current_date" "$script_name" "$message" >> /var/log/messages
}
loggerd
